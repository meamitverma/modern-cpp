#include<iostream>


int main(){
    
    // int32_t int32_t_res = div(prod(sub(sum12( 20)));

    // functional composition (actual)
    // handle = compose(div, prod, sub, sum);
    // handle(12, 30);

    return 0;
}